# Unattributed 08 downloads

`08-seeds-and-augmentation__1ca611f-32cell.ipynb` records `HEAD is now at 1ca611f`, the
same code commit as [`v4_2026-08-02T0421_1ca611f/`](../v4_2026-08-02T0421_1ca611f/), but is
a **32-cell** notebook where v4's is 33, so it is not the same download. It carries the same
per-run PSNRs (v12/42 38.659, winner/42 37.230, winner_aug/42 38.939) — expected, since
every 08 version after the first reuses the same twelve checkpoints and its training numbers
therefore never change.

Which Kaggle version it is cannot be determined from its contents, so it is parked here
rather than filed under a version it might not belong to.
